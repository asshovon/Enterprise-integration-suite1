# PORT: The port to run the development server and backend on.
PORT=3000

# APP_URL: The URL where this applet is hosted locally.
# Used for local self-referential links, OAuth callbacks, and API endpoints.
APP_URL="http://localhost:3000"
